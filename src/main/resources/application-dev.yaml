spring:
  datasource:
    url: jdbc:h2:mem:discodeit_dev
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

logging:
  level:
    root: debug
    org.hibernate.sql: debug